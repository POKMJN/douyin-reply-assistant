## 抖音回复助手 v2.0.0（深度内存压缩、防截断、防串台错发与天气晨报通顺度优化版）

> **【2026-09-25 城市天气精准解析闭环与自动回复 Worker 永不断链保活重构】**
> 1. **城市天气识别与前端测试全链路修复**：
>    - **前端接口判定兼容**：修复前端设置页“测试天气”按钮对后端返回字段的判定（旧版只校验 `res.success`，后端返回 `ok: true` 导致前端误判并弹红框“无法解析天气”），全面兼容 `res.success || res.ok`，并提供实时解析预览；
>    - **后端城市参数直通透传**：`ai:get-weather` 与 `fetchWeatherContext` 支持 `targetCity` 优先穿透，实时测试即时锁定输入城市，不再受历史缓存或未保存配置干扰；
>    - **大模型 Prompt 显式锁定城市名**：在晨间续火花 Prompt 中注入明确指导规则——“若天气指明具体城市（如银川），在问候中必须自然带上城市名与体感（如‘银川今天最高有27度挺凉爽’），绝不能抹去城市名”，并在 Few-shot 示范中增加标准城市范例，彻底根除大模型自作主张吞掉城市名的问题。
> 2. **自动回复 Worker 永不断链与 90 秒硬超时自愈熔断**：
>    - **根因查清**：旧版 `startWorker()` 中的 `runAutomation()` 如果由于 Electron 隐藏窗口 `executeJavaScript` 异常或视频抓取挂起，底层 Promise 永久 pending；原有的 5 分钟软看门狗仅把 `polling` 置 false，根本无法 abort 底层 Promise，也从未调用 `scheduleNext()`，导致调度链条彻底断裂，自动回复永久假死且无任何日志；
>    - **90 秒单轮硬超时熔断（Promise.race）**：在 `startWorker()` 外层调度增加 90 秒硬超时包装，超时时自动熔断并调用 `recycleChatWindow()` 强制销毁卡死的渲染窗口，释放死锁与内存；
>    - **Unbreakable 调度保活闭环**：`scheduleNext()` 移入 `finally` 保障块，无论单轮发生何种超时、异常或正常结束，定时器调度链条 100% 永不断链；
>    - **DOM 交互硬超时注入**：为所有的 `executeJavaScript` 注入 `safeExecuteJavaScript` 3 秒硬超时保护，从源头杜绝未响应卡死。
> 3. **用户个人设置 100% 隔离保护**：
>    - 严格遵循零配置侵入原则，所有优化完全在底层应用执行器与协议层完成，严禁触碰或覆盖用户的个人配置与数据。
>
> ---
>
> **【2026-09-24 会话切换防串台错发与晨间播报执行熔断彻底修复】**
> 1. **根治会话切换“串台发错人”恶性漏洞**：
>    - **根因查清**：旧版 `selectConversation` 点击联系人后，判定条件仅检查页面是否存在输入框且右侧非空，若前一个联系人的聊天窗口已打开，首轮检查瞬间误判为“切换成功”，导致给下一个联系人的晨间问候直接误打并发送进上一个联系人的窗口（如现场日志中【情到终时难自遣】连续收到本属于【勤.】和【②】的播报）；
>    - **会话目标强校验与重试重连**：重构 `selectConversation`，严格校验左侧激活项（`activeRow`）与右侧聊天面板 Header 标题必须与目标联系人严格匹配。若检测到当前仍停留在其他联系人窗口，坚决禁止放行，并通过 DOM/鼠标事件双通道自动重试切换；
>    - **`sendMessage` 发送前二度防御断言（Double Guard）**：在消息写入输入框并按下发送前，执行物理断言，若当前激活窗口不匹配目标联系人立即安全熔断拒发，杜绝任何情况下的串发事故。
> 2. **晨间播报与连续任务防抖与安全冷却**：
>    - 在连续触发多个晨间播报任务时加入 1.5 秒平稳安全间隔，等待前端 DOM 完全闭环再处理下一任务，避免高频并发导致的前端渲染阻塞与错位。
>
> ---
>
> **【2026-09-24 城市天气识别与多账号配置隔离彻底修复】**
> 1. **多账号城市配置隔离与全局继承**：
>    - 针对用户配置了城市（如“银川”）后，后台子账号或切换账号运行续火花时仍为空导致按网络 IP 误判的问题，引入跨账号全局城市（`globalWeatherCity`）机制与启动自动同步，确保任一账号配置城市后，所有后台任务与并发服务 100% 统一生效。
> 2. **天气城市名称解析与英文地名过滤修复**：
>    - 修复 wttr.in 英文地名返回（如 `Yinchuan`）被中文正则过滤误丢弃的问题，精准锁定用户配置的目标城市名并生成规范的体感天气文案（如 `银川今天 18~25°C，阴`）；
>    - 完善天气中文翻译映射，提升干燥、微风、扬沙等多类型气象描述准确度。
> 3. **天气缓存多维度隔离与即时测试**：
>    - 将按日缓存 key 升级为 `日期+城市名` 联合维度，彻底根除修改城市后整天被旧城市或旧 IP 缓存污染的问题；
>    - 设置页面中【天气城市】增加【测试天气】即时测试按钮与实时天气预览反馈，配置城市后可即时一键验证抓取到的真实天气和温度。
>
> ---
>
> **【2026-09-23 晨间 AI 续火花语句通顺度深度提升与静默启动】**
> 1. **晨间 AI 续火花（今日播报）语句通顺度全面重构**：
>    - **根因分析**：旧版 Prompt 将日期、节日、天气、热点、祝福作为 5 项独立清单要求模型依次罗列，导致生成语句容易呈现“天气预报报幕”或“早报摘要拼接感”（如：*“今天是X月X日星期X，气温XX度降水XX出门带伞，刚在热搜看到XX，祝你XX”*），生硬且缺乏真人微信/抖音朋友闲聊的自然呼吸感与人情味；
>    - **有机融合流重构**：打破生硬列表罗列，重构 Prompt 结构，引导大模型按真人第一人称视角，将时段问候、生活化天气关照、随口闲聊热点与轻快祝福融为 2~3 个自然口语短句；
>    - **注入多场景高分真人示范**：新增雨天/降温关心型、晴天/日常型、热点/闲聊型、节日问候型等标杆 Few-Shot 示例，并建立反向负向警示（严禁机械报日期、客服腔、打卡腔）；
>    - **天气与热点口语化转译**：天气预报数值自动转译为体感关心（如“今天有点降温直接跌到十几度”、“今天大太阳出门记得防晒”），热点以随口分享切入（如“听说……”、“刚瞅见……”）；
>    - **兜底文案温度升级**：修复回退逻辑中的变量作用域问题，离线/生成异常兜底文案同步升级为温馨自然的口语关心。
> 2. **新增静默启动（后台最小化到托盘）功能**：
>    - **功能说明**：在设置页“通用配置”中新增【静默启动】选项（支持选择「开启：最小化到托盘」或「关闭：正常弹出窗口」）；
>    - **系统级静默集成**：配置开机自启或通过快捷方式带 `--hidden` 参数启动时，程序启动后完全在后台静默运行并驻留系统托盘，不抢占前台焦点，点击托盘图标或桌面快捷方式即可即时唤醒主界面。
> 3. **彻底修复设置项自动改回默认值缺陷**：
>    - **引入 `deepMerge` 递归深度合并**：重构 `JsonStorage.update()` 与 `migrateLegacy()`，对多层嵌套配置执行深度递归合并，确保只更新被修改的具体字段，其余配置项 100% 完整保留；
>    - **全量回归验证**：149 项测试套件连续 12 轮 100% 全部通过（`149 pass, 0 fail`）。

---

### 下载与安装推荐

- **首次使用推荐安装包**：下载 **DouyinReplyAssistant-Setup-v2.0.0.exe**，双击一键安装并自动创建桌面快捷方式；
- **免安装绿色运行**：下载 **DouyinReplyAssistant-Portable-v2.0.0.zip** 便携版，解压即可运行；
- **老用户免重装极速热更新**：直接下载 **app.asar** 替换安装目录下的 `resources/app.asar`，重启软件即可立即生效。
