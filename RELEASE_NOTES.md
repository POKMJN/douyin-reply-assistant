## 抖音回复助手 v2.0.0（深度内存压缩、防截断、设置深度持久化与静默启动完整版）

> **【2026-09-23 新增静默启动与设置项深度持久化】**
> 1. **新增静默启动（后台最小化到托盘）功能**：
>    - **功能说明**：在设置页“通用配置”中新增【静默启动】选项（支持选择「开启：最小化到托盘」或「关闭：正常弹出窗口」）；
>    - **系统级静默集成**：配置开机自启或通过快捷方式带 `--hidden` 参数启动时，程序启动后将完全在后台静默运行并驻留系统托盘，不抢占前台焦点，点击托盘图标或桌面快捷方式即可即时唤醒主界面；
>    - **开机自启注册表同步**：开启静默启动后，开机自启注册表项自动注入静默启动参数，实现开机完全无感启动。
> 2. **彻底修复设置项自动改回默认值缺陷**：
>    - **根因分析**：前端在修改单个设置项（如开启开机自启、免打扰或调整每日上限）时，仅向上发送包含该项的增量补丁（Patch）。旧版主进程存储层在合并状态时执行了浅层扩展（`{ ...this.state, ...patch }`），导致整组配置被单项局部对象覆盖，其他既有配置被擦除并在冷启动时回退至系统预设默认值；
>    - **引入 `deepMerge` 递归深度合并**：重构 `JsonStorage.update()` 与 `migrateLegacy()`，对配置对象与多层嵌套对象（如 `settings.proactiveChat`）执行深度递归合并，确保只更新被修改的具体字段，其余配置项 100% 完整保留；
>    - **自动化测试套件全量通过**：扩充存储层深度合并与静默启动单元测试，149 项测试套件 100% 通过（`149 pass, 0 fail`）。
>
> **【2026-09-21 巡检问题集中修复与热补丁】**
> 1. **彻底修复 AI 标签重复叠加与正文截断缺陷**：历史上下文标签清洗、改写 Token 预算扩充至 180 tokens、残缺标签与未收束标点过滤、外层标签防重守卫。
> 2. **深度内存压缩机制（物理内存降幅达 80%~85%）**：Chromium 参数收敛（renderer-process-limit: 3）、多账号空闲按需唤醒休眠、Windows 原生 EmptyWorkingSet 系统级工作集修剪。
> 3. **彻底修复晨间播报（续火花）被截断成残句问题**：token 预算放宽至 3500 tokens，增加悬挂词残句截断检测与平滑收尾。
> 4. **阻断长消息截断引发的重复回复死循环**：增加出站回声双向守卫。

---

### 下载与安装推荐

- **首次使用推荐安装包**：下载 **DouyinReplyAssistant-Setup-v2.0.0.exe**，双击一键安装并自动创建桌面快捷方式；
- **免安装绿色运行**：下载 **DouyinReplyAssistant-Portable-v2.0.0.zip** 便携版，解压即可运行；
- **老用户免重装极速热更新**：直接下载 **app.asar** 替换安装目录下的 `resources/app.asar`，重启软件即可立即生效。
